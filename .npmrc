# Tell npm to always install optional dependencies (this is the default,
# but being explicit ensures cross-platform optional deps like
# lightningcss-linux-x64-gnu are installed on the deployment server).
include-optional=true
